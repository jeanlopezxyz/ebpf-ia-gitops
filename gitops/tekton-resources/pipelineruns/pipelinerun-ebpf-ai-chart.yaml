# PipelineRun template - not auto-executed to avoid race conditions
# To execute: kubectl create -f this-file or use Tekton Dashboard
apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  name: ebpf-ai-chart-update
  namespace: tekton
  annotations:
    argocd.argoproj.io/sync-wave: "1"  labels:
    app.kubernetes.io/name: tekton-ci
    app.kubernetes.io/instance: tekton-ci
    app.kubernetes.io/component: pipelinerun-ebpf-ai-chart
    app.kubernetes.io/part-of: ebpf-ai-system
spec:
  pipelineRef:
    name: helm-charts-ci
  workspaces:
    - name: shared-data
      volumeClaimTemplate:
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 1Gi
  params:
    - name: git-url
      value: https://github.com/jeanlopezxyz/ebpf-ia-gitops.git
    - name: git-revision
      value: main
    - name: charts-path
      value: helm/charts
    - name: commit-message
      value: "Update eBPF-AI chart dependencies and .tgz files"
